channels:
  - conda-forge
  - bioconda
dependencies:
  # CheckV 1.1+ is the current release. Confirm the installed CheckV
  # database (config CHECKV_DB) is a version CheckV 1.1 accepts before a
  # production run; older databases may need refreshing with
  # `checkv download_database`.
  - checkv>=1.1.1
