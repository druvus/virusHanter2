channels:
  - conda-forge
  - bioconda
dependencies:
  - bwa>=0.7.17
  - samtools>=1.18
  # bwa_align_to_kraken_hits is now a `script:` that reads the
  # VIRUS_PARQUET, walks the NCBI taxdump for ICTV species names,
  # and writes the virus_names sidecar. Needs python + pandas +
  # pyarrow (parquet) at runtime.
  - python>=3.12
  - pandas>=3
  - pyarrow
  - numpy>=1.24
