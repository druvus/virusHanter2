channels:
  - conda-forge
  - bioconda
dependencies:
  - kaiju>=1.10.1
  # kaiju_to_table rule is now a `script:` that post-processes the
  # TSV via canonicalise_taxon_names (scripts/functions.py). Add the
  # Python deps that helper needs.
  - python>=3.12
  - pandas>=3
  - numpy>=2.5.0
